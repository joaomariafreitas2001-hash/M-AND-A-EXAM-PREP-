/** Paste your Google Apps Script web app URL and secret after setup (see TELEMETRY-SETUP.md). */
const TELEMETRY = {
  /** Full URL ending in /exec — leave empty to disable logging */
  webhookUrl: '',
  /** Same secret as in Apps Script — stops casual spam only (visible in page source) */
  token: 'change-me-to-a-long-random-string',
};
